{
  "name": "fig2code",
  "private": true,
  "version": "0.0.0",
  "description": "Fig2Code — Figma plugin that generates production-ready code into your team's repo",
  "scripts": {
    "build": "turbo run build",
    "build:api": "turbo run build --filter=@fig2code/api...",
    "build:worker": "turbo run build --filter=@fig2code/worker...",
    "start:api": "pnpm --filter @fig2code/api start",
    "start:worker": "pnpm --filter @fig2code/worker start",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20"
  }
}
