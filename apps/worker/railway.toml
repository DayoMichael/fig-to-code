# Railway config for the worker service.
# In the service Settings → Config-as-code, set: /apps/worker/railway.toml

[build]
buildCommand = "pnpm turbo run build --filter=@fig2code/worker..."

[deploy]
startCommand = "pnpm start:worker"
